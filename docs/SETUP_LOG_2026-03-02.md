# Linen Portal — Kurulum / Yapılanlar Log (2026-03-02)

Bu doküman, Linen Portal projesinde bugün yaptığımız kurulum/konfig ve UI geliştirmelerini adım adım özetler.

---

## 1) Tema / Token (CSS Variables) Düzenleri

### 1.1 Accent (turuncu) setlendi

- --accent: #C05B2F;
- --accent-foreground: #ffffff;

Temel token’lar:

- --background: #F8F6F2;
- --foreground: #18150F;
- --card: #ffffff;
- --card-foreground: #18150F;
- --border: #E6E1DB;

---

## 2) Dashboard

Dosya: src/app/dashboard/page.tsx

- Supabase projects list
- Desc order by created_at
- Badge ile proje sayısı
- Token tabanlı stil

---

## 3) Batch Review Page

Dosya: src/app/projects/[projectId]/batches/[batchId]/page.tsx

- images tablosundan batch filtreli çekim
- created_at ascending
- Toplam image count
- pending / approved / revision count hesapları

---

## 4) Gallery Component

Dosya: src/app/projects/[projectId]/batches/[batchId]/Gallery.tsx

### Thumbnail
- Sadece AFTER görsel
- Status chip
- Pretty filename logic

### Modal Compare
- Before/After slider (clipPath)
- ESC ile kapanma

### Save Flow
- updateImageStatus(id, status)
- router.refresh()
- isSaving state
- 400ms sonra modal auto-close
- Optimistic UI update

---

## 5) Vercel Build Fix

Hata:
Expected 2 arguments, but got 3.

Çözüm:
updateImageStatus çağrısı 2 parametreye indirildi.

---

## 6) Git Akışı

git add .
git commit -m "Batch review modal save + auto close 400ms"
git push

---

## TODO

- client_note DB’ye yazma
- Approved ikon polish
- Download ZIP backend
- Token sistemini tek kaynağa toplama

---

# 2026-03-04 Çalışma Özeti (Retouchio UI → Linen Portal)

## DONE
- Retouchio tema/token sistemi Linen Portal’a taşındı (renk/typography/spacing/shadcn uyumu).
- Gallery/Review ekranı UI revizyonları: mobil KPI chip, foto odaklı grid, modal/top-sheet denemeleri.
- Status güncelleme bug’ı düzeldi (saving sonrası pending’e dönme problemi giderildi).
- Supabase env prod crash fix: `src/lib/supabase.ts` dot-notation ile client bundle inline sorunu çözüldü.
- Landing `/` login sayfası + giriş/kayıt toggle eklendi.
- Vercel prod deploy: `linen-portal.vercel.app`.

## TODO (Yarın)
- Login sonrası akış: `/app/batches` → otomatik ilk/son batch gallery’ye redirect (karar verilecek).
- Batch preview/gallery: compare/detail açılışı “modal gibi” olmasın (inline panel veya ayrı route kararı).
- Lightbox/Detail UX:
  - “Save” butonunu kaldır (status seçimi zaten kaydediyor).
  - Modal/top-sheet yüksekliği ve boşlukları hizala (ekran yüzdesi dengeli).
- Compare slider mouse drag hâlâ kararsız (pointer/overlay çakışması); ayrıca ele alınacak.
- Supabase email doğrulama redirect: Auth URL Configuration’da Site URL + Redirect URLs doğru ayarlanacak.
- `/showcase` içeriği doldurulacak (Retouchio showcase benzeri).

