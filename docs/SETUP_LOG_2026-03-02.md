# Linen Portal — Kurulum / Yapılanlar Log (2026-03-02)

Bu doküman, Linen Portal projesinde bugün yaptığımız kurulum/konfig ve UI geliştirmelerini adım adım özetler.

---

## 1) Tema / Token (CSS Variables) Düzenleri

### 1.1 Accent (turuncu) setlendi

- --accent: #C05B2F;
- --accent-foreground: #ffffff;

Temel token’lar:

- --background: #F8F6F2;
- --foreground: #18150F;
- --card: #ffffff;
- --card-foreground: #18150F;
- --border: #E6E1DB;

---

## 2) Dashboard

Dosya: src/app/dashboard/page.tsx

- Supabase projects list
- Desc order by created_at
- Badge ile proje sayısı
- Token tabanlı stil

---

## 3) Batch Review Page

Dosya: src/app/projects/[projectId]/batches/[batchId]/page.tsx

- images tablosundan batch filtreli çekim
- created_at ascending
- Toplam image count
- pending / approved / revision count hesapları

---

## 4) Gallery Component

Dosya: src/app/projects/[projectId]/batches/[batchId]/Gallery.tsx

### Thumbnail
- Sadece AFTER görsel
- Status chip
- Pretty filename logic

### Modal Compare
- Before/After slider (clipPath)
- ESC ile kapanma

### Save Flow
- updateImageStatus(id, status)
- router.refresh()
- isSaving state
- 400ms sonra modal auto-close
- Optimistic UI update

---

## 5) Vercel Build Fix

Hata:
Expected 2 arguments, but got 3.

Çözüm:
updateImageStatus çağrısı 2 parametreye indirildi.

---

## 6) Git Akışı

git add .
git commit -m "Batch review modal save + auto close 400ms"
git push

---

## TODO

- client_note DB’ye yazma
- Approved ikon polish
- Download ZIP backend
- Token sistemini tek kaynağa toplama
